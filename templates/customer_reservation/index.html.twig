{% extends 'base.html.twig' %}

{% block title %}Reservation index{% endblock %}

{% block body %}
    <div class="card" style="margin: 10px">
        <div class="card-header"><b>Mes réservations</b></div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered">
                    <thead>
                    <tr>
                        <th>Couette & Cafe</th>
                        <th>Propriétaire</th>
                        <th>Contact</th>
                        <th>Date de début</th>
                        <th>Date de fin</th>
                        <th>Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for reservation in reservations %}
                        <tr>
                            <td>{{ reservation.room }}</td>
                            <td>{{ reservation.room.owner.firstname }} {{ reservation.room.owner.familyName }}</td>
                            <td>{{ reservation.room.owner.user }}</td>
                            <td>{{ reservation.startDate|date("m-d-Y") }}</td>
                            <td>{{ reservation.endDate|date("m-d-Y") }}</td>
                            <td>
                                {{ include('customer_reservation/_delete_form.html.twig') }}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8">Vous n'avez aucune réservation...</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}
