<div class="card-body">
    {{ form_start(form) }}
    {{ form_widget(form) }}
</div>
<div class="card-footer">
    <div class="row">
        <div class="col align-self-start">
            <a href="{{ path('unavailability_index') }}" role="button" class="btn btn-secondary">Cancel</a>
        </div>
        <div class="col align-self-start">
            <button type="submit" class="btn btn-success">{{ button_label|default('Save') }}</button>
        </div>
        {{ form_end(form) }}
        <div class="col align-self-end">{{ include('unavailability/_delete_form.html.twig') }}</div>
    </div>
</div>