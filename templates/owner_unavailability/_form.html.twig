<div class="card-body">
    {{ form_start(form) }}
    {{ form_row(form.endDate) }}
    {{ form_row(form.startDate) }}
    {{ form_row(form.room) }}
</div>
<div class="card-footer">
    <div class="row">
        <div class="col">
            <a href="{{ path('owner_unavailability_index') }}" role="button" class="btn btn-secondary"
               style="margin: 5px">Cancel</a>
        </div>
        <div class="col">
            <button class="btn btn-success" style="margin: 5px">{{ button_label|default('Save') }}</button>
        </div>
    </div>
    {{ form_end(form) }}
</div>
