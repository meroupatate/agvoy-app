<div class="card-body">
    {{ form_start(form) }}
    {{ form_widget(form) }}
</div>
<div class="card-footer">
    <div class="row">
        <div class="col align-self-start">
            <a href="{{ path('reservation_index') }}" role="button" class="btn btn-secondary">Cancel</a>
            <button type="submit" class="btn btn-success">{{ button_label|default('Save') }}</button>
        </div>
        {{ form_end(form) }}
        <div class="col align-self-end">{{ include('reservation/_delete_form.html.twig') }}</div>
    </div>
</div>